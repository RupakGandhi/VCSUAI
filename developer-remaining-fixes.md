# VCSU remaining fixes — September 20, 2026

The latest build is improved. The unrelated-follow-up rejection, cross-module Higher Ed role refresh, Higher Ed module certificate, Apply readability/pairing and targeted keyboard controls pass the tested live routes. Current handler tests also pass upload follow-up binding and pending-verification role/module guards. Please preserve those repairs.

**Trainer handoff is still blocked by the following items.** Use the existing architecture and preserve current curriculum, IDs, roles, client scope and working behavior. No new AI integration is requested.

| Item | Exact reproduction / current evidence | Required result |
|---|---|---|
| **1. Sample files — D05/D11** | Open any of the 14 current Download sample file links: each renders 404 / Not Found. D1.1 Teacher → Testable needs-assessment hypothesis and T3.1 Teacher → Inclusive newsletter and belonging routine, using retained matching files, both fail to load the reference. Fresh static ZIP contains zero sample files. | Restore the actual matching assets in the live and exported workflows. For all 14 activities, download/open/upload the real sample and run its exact case plus three follow-ups. Correct file succeeds; materially modified same-name, unrelated, empty and unsupported files reject; missing reference stops. Check both T2.3 Support Staff samples independently. Root cause of absent files has not been established. |
| **2. Facilitator access/export — D10** | Secure live unlock displayed Incorrect password, so guide/pathway testing could not proceed. Source now restores all 31 export buttons and filters roles, but still takes only the first heading per section, groups paragraphs/lists out of original order and does not fully paginate oversized blocks. | Authorized access is needed to finish the live export checks. Selected-role PDFs must preserve the visible D1.1 and T4 guide content: every heading, Close the Loop, instructional order and all text without clipping. Keep the new role filtering, controls and page-height fixes. |
| **3. Pathway membership — D07** | Instructional Design selects T2.1–T2.3 despite promising lesson planning/differentiation/workflow. Assessment and Feedback selects T3.1–T3.3, currently SEL/behavior modules. These are current source findings; live routes remain gated. | Use approved current modules for the stated training purpose and align screen, agenda, links and PDF. T1.1–T1.3 fit Instructional Design; T2.1/T2.2/T2.4 cover assessment/feedback/integrity. Include T2.3 only if intended and adjust the agenda accordingly. |
| **4. Pathway consistency — D07** | Student-Centered agenda totals 335 minutes; displayed 5.5 hours is 330. Systems/Governance screen/PDF audiences differ. | Agree on duration and audience once and show them consistently. Preserve corrected Quick Start 160-minute timing and Systems/Governance 470-minute timing. The previous 455-minute Systems audit calculation is withdrawn. |
| **5. Three rubric responses — content owner** | T2.4 Higher Ed → Redesign a disciplinary assessment still returns only five rubric trait names; live screenshot shows the completed response. Instructional Coach → Coach an assignment redesign and Teacher → Build an AI-inclusive assessment packet remain unchanged in CMS. | Add defined scoring levels/descriptors and a worked scoring example for each supplied case. Define what earns 0/1/2 where that scale is already promised. Keep existing case facts, outcomes and accessibility/human-review provisions. |
| **6. Survey button state — D06** | Fresh T4.2, T4.1 and T4.3 Higher Ed 3/3 surveys reached the correct VCSU records; completion, reload persistence and exclusion work. After completing T4.2, open T4.1 Apply → Mark Module Complete: the new survey has cleared inputs but already says “Submitting...” on its button. | Reset the idle button label/state for every new module survey and show the loading label only while that submission is pending. Later submissions still succeed; this is a stale-label defect, not an HTTP failure. Preserve working success, completion, exclusion and analytics behavior. |
| **7. Course certificate activation — D09** | Complete T4.1–T4.3 → open T4.3 → top-right next arrow → Course Complete → Download Course Certificate. Two activations produced no tab, file, dialog or visible output. Module certificate opening works. | Bind the actual rendered end-panel controls after course reconciliation so the button invokes the intended certificate action. Source review indicates earlier property handlers are lost when the T4 end panel is cloned later. Adapt the repair to the existing architecture. Its absence from overview/home is expected, not a defect. Separately, source routes the final D2 module directly home, skipping its end panel; keep its completion/certificate route reachable. D2 was not tested live. |
| **8. Residual onboarding wording/editability — D04** | Major visible copy is corrected. Gemini still says any Google account; generic free-account wording can imply universal availability. CMS still does not replace the complete static tool/tip cards. | Qualify eligible institutional access, including administrator enablement for school accounts; keep optional tools optional. Ensure supported CMS edits update the intended full content without stale children. Avoid requiring a new account/model-selection workflow. |

## Evidence provided

- D1.1-Sample-404.jpg; D1.1-Matching-Sample-Still-Fails.jpg; T3.1-Matching-Sample-Still-Fails.jpg.
- T2.4-HigherEd-Rubric-Result.jpg — actual completed live answer, not merely the stored CMS text.
- T4-Course-Certificate-No-Output.jpg and T4-course-certificate-button-after-click.txt show the activated course control. Survey-Second-Module-Stale-Submitting.jpg shows the stale label on a new survey.
- T4.2-HigherEd-Certificate.jpg and Survey-Keyboard-Accessible.jpg show repaired routes. T4.2-unrelated-continuation.txt records the actual unsupported-input rejection and T4.2-HigherEd-followup.txt records the subsequent correct reply.
- sample-links-live.json and sample-url-results.json list all exact current URLs and observed 404 pages.
- The full retest report separates live tests, source findings, controlled handler tests and blocked checks.

## Acceptance limits and handling

The following remain acceptance checks, not reproduced defects: backend receipt semantics, genuine survey failure/retry, duplicate prevention, cross-client isolation, an older completion date after revisit, actual PDF formatting, clean standalone/offline/cache-update execution and broader browser/mobile coverage. Required behavior is retained survey answers and truthful completion, one accepted record per intended submission, correct client scope, accurate certificate information and working packaged assets. Current source checks HTTP success without reading a receipt body; the backend contract was not available in the export.

All 930 current live Practice/Mastery text fields match CMS. Controlled tests exercise 558 primary and 1,674 follow-up replies, plus all 14 sample bindings; that is not the same as all workflows tested live or all 558 external Apply artifacts produced. Once files and facilitator access work, complete the blocked sample and guide/pathway tests. Remaining trainer demonstrations must use the actual approved tools/accounts and verify saved artifacts.

All three September 20 QA surveys are preserved but individually excluded from participant evaluation, with total participant responses zero. Learner completion persists at 3 of 31 after reload. No data was deleted. No curriculum was reimported or changed and no message was sent to Qaisar during this retest.

Keep unrelated curriculum and working functionality unchanged; implement the required behavior in the existing system.
